/* v-cross2.f
 * MODULE
        Название Программного Модуля
 * DESCRIPTION
        Назначение программы, описание процедур и функций
 * RUN
        Способ вызова программы, описание параметров, примеры вызова
 * CALLER
        Список процедур, вызывающих этот файл
 * SCRIPT
        Список скриптов, вызывающих этот файл
 * INHERIT
        Список вызываемых процедур
 * MENU
        Перечень пунктов Меню Прагмы 
 * AUTHOR
        31/12/99 pragma
 * CHANGES
*/

/* v-cross2.f  02/03/94 - AGA      */


def var vtrx as log  label "Druk–t pec Kods/Nos." format "Kods/Nos.".
def var cr2  as char format "x(7)" init "laiks: ".
def var cr3  as char format "x(28)" init "VAL®TU  CROSS-RATES   uz    ".
def var cr4  as char format "x(15)" init "SKAIDR… naud–  ".
def var cr5  as char format "x(5)" init  "Par  ".
def var cr6  as char format "x(16)" init "P–rёjam  par  1 ".
def var cr7  as char format "x(15)" init "Klients   PЁRK ".
def var cr8  as char format "x(16)" init "Klients   P…RDOD".
def var cr9  as char format "x(16)" init "BEZSKAIDR… naud–".
def var cr10 as char format "x(20)" init "  DOKUMENTA BEIGAS  ".
